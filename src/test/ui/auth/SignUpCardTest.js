const test = require("unit.js");
import { SignUpCard } from "../../../main/ui/auth/SignUpBlock";

class SignUpBlockTest {
  constructor() {
    const block = new SignUpBlock();
    // describe("Test if is username valid", () => {
    //   it("", () => {
    //     test.assert( throw(), block.isUsernameValide());
    //   });
    // });
  }
}
